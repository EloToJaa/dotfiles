name: Trigger Build on PR

on:
  workflow_run:
    workflows: [Flake ❄️ Lock 🔒️ Updater ✨]
    types: [completed]

jobs:
  trigger-build:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    uses: ./.github/workflows/build.yml
    secrets: inherit
